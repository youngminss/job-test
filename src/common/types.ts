export interface Gender {
  MALE: number;
  FEMALE: number;
}

export interface TestContent {
  queryNumber: number;
  text: string;
}

export interface TargetSerial {
  targetSerial: number;
  text: string;
}
